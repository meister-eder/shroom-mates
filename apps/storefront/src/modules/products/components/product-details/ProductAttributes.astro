---
interface Props {
  material?: string | null;
  origin_country?: string | null;
  type?: { value: string } | null;
  weight?: number | null;
  length?: number | null;
  width?: number | null;
  height?: number | null;
}

const { material, origin_country, type, weight, length, width, height } =
  Astro.props;
---

<div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
  <div class="grid grid-cols-2 gap-x-8">
    <div class="flex flex-col gap-y-4">
      <div>
        <span class="font-semibold">Material</span>
        <p>{material || "-"}</p>
      </div>
      <div>
        <span class="font-semibold">Herkunftsland</span>
        <p>{origin_country || "-"}</p>
      </div>
      <div>
        <span class="font-semibold">Typ</span>
        <p>{type?.value || "-"}</p>
      </div>
    </div>

    <div class="flex flex-col gap-y-4">
      <div>
        <span class="font-semibold">Gewicht</span>
        <p>{weight ? `${weight}g` : "-"}</p>
      </div>
      <div>
        <span class="font-semibold">Maße</span>
        <p>
          {
            length && width && height
              ? `${length}L x ${width}B x ${height}H`
              : "-"
          }
        </p>
      </div>
    </div>
  </div>
</div>
